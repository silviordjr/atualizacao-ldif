const totalList = (finalList, wrongList) => {
    const users = []
    
    finalList.map((user) => {
        users.push(user)
    })

    wrongList.map((user) => {
        users.push(user)
    })

    return users
}

export default totalList